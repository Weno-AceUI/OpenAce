# CMakeLists.txt for UI assets directory

# This can be used to group asset-related subdirectories

add_subdirectory(css)
add_subdirectory(js)
# Add other asset types here, e.g., fonts, images, if they have their own CMakeLists.txt